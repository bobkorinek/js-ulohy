/**
 * Funkce, která vrátí číslo, které představuje počet přechodů, které je
 * potřeba, aby pole mělo všechna čísla od nejmenšího čísla až do největšího
 * čísla v seznamu.
 *
 * Příklad: vstup: [6, 2, 3, 8]; výstup: 3; Výsledek je 3, protože je potřeba
 * vyplnit celkem 3 přechody, aby seznam měl všechna čísla od 3 do 8. Tyto
 * přechody jsou 4, 5 a 7.
 *
 * @param {number[]} statues
 * @returns {number}
 */
function makeArrayConsecutive(statues) {
    return 0;
}

exports.makeArrayConsecutive = makeArrayConsecutive;
